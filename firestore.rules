rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the server via Firebase Admin SDK,
    // which bypasses these rules. Lock down direct client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
