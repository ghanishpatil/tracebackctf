{
  "name": "ctf-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Jeopardy-style CTF Platform",
  "workspaces": [
    "client",
    "shared",
    "functions"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client",
    "serve": "npm run preview --workspace=client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
